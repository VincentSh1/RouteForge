global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - deploy/observability/prometheus/alerts.yml

scrape_configs:
  - job_name: routeforge
    static_configs:
      - targets:
          - 127.0.0.1:9090
