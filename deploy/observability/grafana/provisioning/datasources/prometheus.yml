apiVersion: 1

prune: true

datasources:
  - name: RouteForge Prometheus
    type: prometheus
    uid: routeforge-prometheus
    access: proxy
    url: ${GRAFANA_PROMETHEUS_URL}
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 15s
